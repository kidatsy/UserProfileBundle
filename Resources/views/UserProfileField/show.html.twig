{% extends 'CrisisTextLineUserProfileBundle::base.html.twig' %}

{% block userprofiles %}
    <h1>UserProfileField</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Default Value</th>
                <td>{{ entity.defaultValue }}</td>
            </tr>
            <tr>
                <th>Section</th>
                <td><a href="{{ path('user_profile_section_show', { 'id': entity.section.id }) }}">{{ entity.section }}</a></td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entity.type }}</td>
            </tr>
            <tr>
                <th>Readaccess</th>
                <td>{{ entity.readAccess }}</td>
            </tr>
            <tr>
                <th>Writeaccess</th>
                <td>{{ entity.writeAccess }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('user_profile_field') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('user_profile_field_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
