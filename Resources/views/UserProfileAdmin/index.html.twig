{% extends 'CrisisTextLineUserProfileBundle::base.html.twig' %}

{% block userprofiles %}
    <h1>UserProfile list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>User</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('user_profile_show', { 'id': entity.user.id }) }}">{{ entity.user.name }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('user_profile_show', { 'id': entity.user.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('user_profile_edit', { 'id': entity.user.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('user_profile_admin_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
